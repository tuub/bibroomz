# Generated by review/review-app.sh; manual edits are overwritten.
APP_NAME="BibRoomz (${REVIEW_SLUG})"
APP_ENV=staging
APP_KEY="${app_key}"
APP_DEBUG=false
APP_URL="${app_url}"
LOG_CHANNEL=daily

DB_DATABASE="${db_name}"
DB_USERNAME="${REVIEW_DB_USERNAME}"
DB_PASSWORD="${REVIEW_DB_PASSWORD}"
DB_HOST="${REVIEW_DB_HOST}"

REDIS_PASSWORD="${REVIEW_REDIS_PASSWORD}"
REDIS_DB=0
REDIS_CACHE_DB=1
REDIS_PREFIX="review_${REVIEW_SLUG}_database_"
CACHE_PREFIX="review_${REVIEW_SLUG}_cache_"
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis

SESSION_DRIVER=redis
SESSION_COOKIE="review_${REVIEW_SLUG}_session"
SESSION_PATH="${url_path}"
SESSION_SECURE_COOKIE=${secure_cookie}

BROADCAST_CONNECTION=reverb
REVERB_SCHEME=http
REVERB_HOST=127.0.0.1
REVERB_PORT=${port}
REVERB_SERVER_HOST=127.0.0.1
REVERB_SERVER_PORT=${port}
REVERB_APP_ID="review-${REVIEW_SLUG}"
REVERB_APP_KEY="review-${REVIEW_SLUG}"
REVERB_APP_SECRET="${reverb_secret}"

VITE_APP_NAME="BibRoomz (${REVIEW_SLUG})"
VITE_API_URL="${app_url}"
VITE_REVERB_APP_KEY="review-${REVIEW_SLUG}"
VITE_REVERB_PATH="${url_path}/reverb/${port}"

AUTH_METHOD="${auth_method}"
AUTH_API_ENDPOINT="${REVIEW_AUTH_API_ENDPOINT}"
MAIL_MAILER=smtp
MAIL_HOST="${mail_host}"
MAIL_PORT=${mail_port}
IS_TEST_ACCOUNTS_ENABLED=${test_accounts}
DB_SEED_EXAMPLE_INSTITUTION=true
