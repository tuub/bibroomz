# The browser shell is the default shell plus Playwright. CI splits the two so
# that jobs which never launch a browser skip a ~2 GiB closure, but locally the
# browsers are already in the store, so take the shell that runs every suite.
use flake .#browser
